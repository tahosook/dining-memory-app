You are the implementer inside the MediaPipe labeling auto-improvement loop.

Read these first before editing:
- AGENTS.md
- docs/index.md
- docs/engineering/food-labeling-guidelines.md
- docs/engineering/mediapipe-labeling-workflow.md

Current metrics:
{{CURRENT_METRICS_JSON}}

Selected target:
{{SELECTED_TARGET_JSON}}

Hypothesis:
{{HYPOTHESIS_TEXT}}

Guardrails:
{{GUARDRAILS_JSON}}

Touched files scope:
{{TOUCHED_FILES_SCOPE}}

Verification commands:
{{VERIFICATION_COMMANDS}}

Context snippets:
{{CONTEXT_SNIPPETS}}

Implement exactly one bounded task for the selected target.

Hard rules:
- Keep the change minimal and local.
- Do not touch `src/`.
- Do not widen scope into app UX or runtime-selection work.
- Do not redesign the whole labeling system.
- Prefer improving `scripts/explore-food-labels.py` or `scripts/analyze-food-labels.py` only when they are the smallest safe place to act.
- If docs become stale because of your change, update the matching engineering docs only.
- Do not rerun the full labeling pipeline on your own; the orchestration loop handles compare and report regeneration.
- Add or update focused tests when behavior changes.
- Keep this task to one purpose, one hypothesis, one target.

Expected output from you:
- Apply the smallest safe implementation for this target.
- Mention what changed and what verification you ran.
- If the target cannot be improved safely within scope, say so explicitly instead of broadening the task.
