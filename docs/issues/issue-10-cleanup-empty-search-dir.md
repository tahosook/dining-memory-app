# [Cleanup] src/ai/search/ ディレクトリの確認とクリーンアップ（Issue #82）

- **ステータス**: 完了 (Closed)
- **結果**: Git リポジトリ上で未追跡（存在しない）ことを確認し Close 済み

## 概要
`src/ai/search/` ディレクトリのクリーンアップを検討する Issue です。
設計ドキュメント（`docs/architecture/tech-spec.md` および `docs/ux/screen-designs.md`）において、セマンティック検索（semantic search）は現在のスコープ外であり、現行の検索機能は SQLite によるテキスト/フィルタパスのみを利用することが明確に規定されています。

## 現状の課題・確認状況
1. ローカル環境に空ディレクトリ `src/ai/search/` が存在する場合がある。
2. 一方で、Git は空ディレクトリを追跡しないため、リモートリポジトリ（GitHub）上には `src/ai/search/` はコミットされておらず存在しない。
3. リポジトリ上に追跡ファイルが存在しない場合、本 Issue はクリーンアップ対象が存在しない（no-op）状態となります。

## 修正方針・運用
1. Git リポジトリ（リモート）上に `src/ai/search/` が追跡されていないか確認する（`git ls-files src/ai/search`）。
2. リポジトリ上に存在しないことを確認した場合は、不要な作業を行わず本 Issue をクローズする。
3. ローカルに空ディレクトリが残っている場合はローカル側で削除してクリーンにする。

## 受入基準
- [x] リポジトリ上に `src/ai/search/` が存在しないことを確認した場合は本 Issue をクローズする。
- [x] （リポジトリ上に存在していた場合のみ）ディレクトリが削除され、ビルド・テストに影響がないこと。
